sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t olajide/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true olajide/docker-react npm run test -- --coverage
